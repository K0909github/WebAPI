このフォルダは、実務に近いミニ課題を「アルゴリズム → Linux整形 → JSの最小UI → 推論API（FastAPI+SQLite）」の順で解く想定の試験セットです。

- 想定所要時間：合計 5時間（目安）
  - Q1：1時間
  - Q2：1時間
  - Q3：1時間
  - Q4：2時間

## 進め方

1. 各 `q*/problem.md` を読み、実装を開始してください。
2. 模範解答は `q*/solution.*`（または `q*/index.html`, `q4_fastapi/app.py`）にあります。
3. 採点観点は `grading.md` を参照してください。

## 注意

- 外部ネットワークなしでも実行できる前提です（Python依存はローカルに `pip install` で導入）。
- Q4 は FastAPI を固定で使用します。
